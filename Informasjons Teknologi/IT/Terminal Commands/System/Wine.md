
**Wine** stands for **W**ine **I**s **N**ot an **E**mulator.

It lets you run **Windows applications** on **Linux** (and on macOS but that’s… a warzone now without helpers like Crossover/Whisky).

Wine translates Windows system calls → Linux system calls, instead of virtualizing a whole OS.